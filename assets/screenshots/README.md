# Real Screenshot Assets

These files are real screenshots from the Claude Mac app.

They are grouped by:

- `chat/`
- `cowork/`
- `code/`
- `setup/`

These screenshots are used in the example pages for each tab so beginners can see the real interface instead of an AI-made mockup.
